<Application
    x:Class="SandSound.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>
            <Color x:Key="SandColor">#FFF4BE62</Color>
            <SolidColorBrush x:Key="SandBrush" Color="{StaticResource SandColor}" />
            <SolidColorBrush x:Key="CardBrush" Color="#FF202020" />
            <SolidColorBrush x:Key="CardElevatedBrush" Color="#FF292929" />
            <SolidColorBrush x:Key="CardStrokeBrush" Color="#FF343434" />
            <SolidColorBrush x:Key="MutedTextBrush" Color="#FFA9A5A0" />
            <SolidColorBrush x:Key="PageBrush" Color="#FF181818" />

            <Style x:Key="ModeToggleStyle" TargetType="ToggleButton">
                <Setter Property="Background" Value="#FF303030" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="CornerRadius" Value="9" />
                <Setter Property="Padding" Value="18,10" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="MinWidth" Value="96" />
            </Style>
            <Style x:Key="QuietButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#FF303030" />
                <Setter Property="BorderBrush" Value="#FF3D3D3D" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="9" />
                <Setter Property="Padding" Value="14,9" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
